title: Frontier — Legal
description: Privacy Policy, Terms of Service & Support for Frontier by Mindcom Media Ltd
# No theme — a self-contained layout in _layouts/default.html handles styling,
# so the pages carry no "View on GitHub" chrome or repo links.
defaults:
  - scope:
      path: ""
    values:
      layout: default
